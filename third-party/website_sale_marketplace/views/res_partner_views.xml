<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_partner_view_form" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit.hr</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='function']" position="before">
                <field name="is_marketplace_vendor" invisible="parent_id" />
                <field name="is_marketplace_vendor_parent" invisible="not parent_id"/>
                <field name="marketplace_markup"
                       invisible="parent_id or not is_marketplace_vendor"
                       widget="percentage"/>
            </xpath>
        </field>
    </record>
</odoo>