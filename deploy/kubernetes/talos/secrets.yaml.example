# =============================================================================
# Section: G – Example Talos secrets file.
# Generate a real secrets.yaml with `talosctl gen secrets -o secrets.yaml`.
# =============================================================================
# This is a placeholder; the actual file contains encryption keys and
# certificate authorities.  NEVER commit the real secrets.yaml.