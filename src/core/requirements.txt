# =============================================================================
# FILE: src/core/requirements.txt
# =============================================================================
# DESCRIPTION:
#   Python dependencies for the NETTRADES Core LangGraph Service.
#   Also includes dependencies for the nettrades-ui (FastAPI + static HTML/JS).
#
#   UPDATES (2026-08-06):
#     • Upgraded LangChain to v1.3.14
#     • Upgraded LangGraph to v1.2.8
#     • Upgraded FastAPI to v0.136.3
#     • Upgraded Uvicorn to v0.49.0
#     • Upgraded Pydantic to v2.13.4
#     • Removed explicit ag-ui-langgraph (handled by CopilotKit)
#   UPDATES (2026-08-07):
#     • Added nettrades-ui dependencies (FastAPI, Uvicorn, httpx, python-multipart,
#       jinja2, python-jose) using the same version ranges as Core to avoid conflicts.
# =============================================================================

# -----------------------------------------------------------------------------
# Huggingface
# -----------------------------------------------------------------------------
huggingface-hub>=0.20.0

# -----------------------------------------------------------------------------
# Core LangChain & LangGraph (v1.x)
# -----------------------------------------------------------------------------
langchain>=1.3.14,<2.0.0
langgraph>=1.2.8,<2.0.0
langgraph-checkpoint-postgres>=3.0.3,<4.0.0

# -----------------------------------------------------------------------------
# Database Drivers (PostgreSQL + pgvector)
# -----------------------------------------------------------------------------
# psycopg[binary] provides the v3 driver with pre-compiled wheels.
# The "binary" extra is required for the `PostgresSaver` import.
psycopg[binary]>=3.2.3,<4.0.0

# Async driver for FastAPI endpoints
asyncpg>=0.31.0,<1.0.0

# -----------------------------------------------------------------------------
# AI / LLM Providers (OpenAI-compatible)
# -----------------------------------------------------------------------------
langchain-openai>=0.3.4,<0.4.0

# -----------------------------------------------------------------------------
# Web Framework & Server
# -----------------------------------------------------------------------------
# Core LangGraph service
fastapi>=0.136.3,<1.0.0
uvicorn[standard]>=0.49.0,<1.0.0
pydantic>=2.13.4,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# nettrades-ui (FastAPI + static HTML/JS) – using same versions as Core
# jinja2 is used for templating (login.html)
jinja2>=3.1.0,<4.0.0
# python-jose is used for JWT/OAuth session handling
python-jose[cryptography]>=3.3.0,<4.0.0
itsdangerous>=2.2.0,<3.0.0

# -----------------------------------------------------------------------------
# HTTP Clients (for external API calls)
# -----------------------------------------------------------------------------
aiohttp>=3.9.0,<4.0.0
httpx>=0.27.0,<1.0.0

# -----------------------------------------------------------------------------
# Image Processing (Vision Agent)
# -----------------------------------------------------------------------------
pillow>=10.0.0,<11.0.0

# -----------------------------------------------------------------------------
# MQTT Client (IoT Integration)
# -----------------------------------------------------------------------------
paho-mqtt>=2.0.0,<3.0.0

# -----------------------------------------------------------------------------
# Observability (Metrics)
# -----------------------------------------------------------------------------
prometheus-client>=0.25.0,<1.0.0

# -----------------------------------------------------------------------------
# Security & Utilities
# -----------------------------------------------------------------------------
python-multipart>=0.0.32,<1.0.0
numpy>=2.4.6
opencv-python-headless
psycopg_pool
psycopg
circuitbreaker>=2.0.0
tenacity>=9.0.0
itsdangerous>=2.2.0,<3.0.0