# =============================================================================
# NETTRADES GPU Agent – Python Dependencies
# =============================================================================
# FILE: src/agent/requirements.txt
#
# PURPOSE:
#   This file lists the Python dependencies required by the NETTRADES
#   GPU Node Agent. The agent runs on each GPU machine to manage
#   registration, WireGuard, and GPUStack workers.
#
# IMPORTANT FIX:
#   Previously, this file was a copy of src/core/requirements.txt and
#   included unnecessary packages (fastapi, uvicorn, langgraph, etc.)
#   while MISSING packages that the agent actually imports (requests,
#   netifaces, etc.).
#
#   FIX: Replaced with the actual dependencies used by the agent.
#
# USAGE:
#   To install the dependencies:
#   pip install -r src/agent/requirements.txt
#
# =============================================================================

# HTTP requests for communicating with Odoo and GPUStack
# Used by: agent.py (registration, health reporting)
requests>=2.28.0

# Network interface detection for hardware-bound node ID
# Used by: agent.py (MAC address fallback for node ID)
netifaces>=0.11.0

# MQTT for IoT integration (optional, but kept for completeness)
# Used by: iot_tools.py
paho-mqtt>=1.6.0

# Process management and system utilities
# Used by: agent.py (subprocess management)
psutil>=5.9.0

# Python-dotenv for environment variable management
# Used by: agent.py (loading .env file)
python-dotenv>=1.0.0

# Note: WireGuard is not a Python package; it's installed separately
# via the system package manager (wireguard-tools on Linux, WireGuard
# installer on Windows).